import * as React from "react"
import { VStack } from "@chakra-ui/react"


type ContractsStatsProps = {
  };

export const ContractStats = (props: ContractsStatsProps) => {

  
  return (
    <> </>
  )
}
